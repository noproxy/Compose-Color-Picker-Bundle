apply plugin: 'maven-publish'

publishing {
    publications {
        release(MavenPublication) {
            groupId = 'com.github.SmartToolFactory'
            artifactId = 'Compose-Color-Picker-Bundle'

            afterEvaluate {
                from components.release
            }
        }
    }
}